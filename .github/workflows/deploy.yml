name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy via webhook
        run: |
          echo "Deploy triggered by GitHub webhook automatically"
          echo "Commit: ${{ github.sha }}"
